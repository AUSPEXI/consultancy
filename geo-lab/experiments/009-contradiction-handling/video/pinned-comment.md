Raw data + full result tables for Experiment 009 are here: [LINK PLACEHOLDER]. To reproduce: take two ~180-word content variants (identical facts/length — one plain, one naming and refuting the target misconception), send the 4 evaluative queries listed in DESIGN.md to gemini-2.5-flash, gpt-4o-mini, perplexity-sonar, and claude-haiku-4-5, at 40 trials per variant per engine (n=320), with source order randomised, and score citation rate per response. Run the CMH stratified test (by query × engine) as the primary endpoint — and report per engine, because pooling triggers a Simpson's paradox here.
