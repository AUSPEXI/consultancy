Raw data, the pre-registered DESIGN.md, and per-engine logs are in the experiment folder linked in the description (Experiment 013). To reproduce: take the two NovaCRM variants (A = statistic buried mid-paragraph; B = same statistic moved to the opening sentence), inject both into the retrievable context with randomised source order, and probe these exact models — gemini-2.5-flash, gpt-4o-mini, sonar, claude-haiku-4-5-20251001 — across the four buyer-intent queries, recording citation rate per engine. Note this run was n=2 per variant per query (16/engine) over a single day, so treat the per-engine numbers as preliminary and ideally re-run at n≥30 over 10+ days before relying on them.
