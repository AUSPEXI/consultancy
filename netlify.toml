# Legacy Vite app — this root netlify.toml is intentionally inert.
# The live site is built from consultancy-next/ (base = consultancy-next set in Netlify UI).
# Do not add redirects here; they would conflict with the Next.js app and break SEO.
